# Testdata Issue Queue

Generated from `../testdata/out/issues` by `go run ./cmd/community-loop`.

Use `make interop ONLY=fixture/id` for fast community-only reruns or `make interop-full ONLY=fixture/id` when fixtures changed.

No synced fixture diffs are queued right now.
